name: build
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Build
       run : |
         touch .env
         echo << EOS
         ${{ vars.multiple_rows }}
EOS >> .env
         cat .env
